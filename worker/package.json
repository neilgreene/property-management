{
  "name": "sdi-ghl-worker",
  "version": "1.0.0",
  "private": true,
  "description": "GoHighLevel integration worker: webhook intake, outbox drain, reconciliation",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test --test-concurrency=1 \"test/**/*.test.js\"",
    "start": "node src/index.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
